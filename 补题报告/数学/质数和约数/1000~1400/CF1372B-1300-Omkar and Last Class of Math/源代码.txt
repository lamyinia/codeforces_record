#include <cstdio>
int T;
/*
	找最小质因数即可
*/
int main(){
	scanf("%d", &T);
	while(T--){
		int n; scanf("%d", &n);
		int key = n;
		for(int i=2;i*i<=n;++i)
		if(n%i == 0){
			key = i;
			break;
		}
		printf("%d %d\n", n/key, n-n/key);
	}
	return 0;
}
